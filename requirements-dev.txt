# os-helper — dev + test requirements.
#
# Derived from pyproject.toml's [dev] extra, the single source of truth CI
# actually installs from (`pip install -e ".[dev]"`). Kept only for
# contributors who prefer `pip install -r requirements-dev.txt` — edit the
# [dev] extra in pyproject.toml, not this file, to change what installs.
-e .[dev]
